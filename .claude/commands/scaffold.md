Read the Stack section of `CLAUDE.md` and `.claude/state/tasks.md`. Generate the initial file/folder structure for the project.

1. Create directories and stub files for the planned architecture.
2. Include boilerplate: config files, .env.example, package.json or pyproject.toml, etc.
3. Set up linting/formatting config consistent with the conventions in CLAUDE.md.
4. Add a minimal working entrypoint (e.g. "hello world" route or script).
5. Ensure it builds and runs out of the box.

After scaffolding, run the build/start command to verify it works. Then update PROGRESS.md.
